# Testing Standards

1. Module isolation — sensor modules tested standalone (e.g. Daytona)
2. Fusion integration — dispatch to module + standardized observation
3. Detection pipeline — cell create/update, scores, status
4. Regression — existing sensors must still pass after new modalities